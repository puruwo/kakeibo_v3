import 'package:flutter/material.dart';

/// ============================================================================
/// 設定画面（config_top）で使用するTextStyleを定義
/// ============================================================================
class ConfigStyles {
  // ==========================================================================
  // ボタン
  // ==========================================================================

  /// エクスポートボタンのテキスト
  ///
  /// 【使用箇所】
  /// - ページ: config_top.dart (設定画面)
  ///   - エリア: 履歴エクスポートエリア
  ///   - 詳細: 「入力履歴をエクスポートする」ボタンのテキスト
  static const TextStyle exportButtonText = TextStyle(
    color: Colors.white,
    // フォントサイズなどが指定されていないためデフォルトを使用
  );
}
